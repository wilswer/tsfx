[build-system]
requires = ["maturin>=1.4,<2.0"]
build-backend = "maturin"

[project]
name = "tsfx"
description = "TSFX -- Time Series Feature eXtraction"
readme = "README.md"
requires-python = ">=3.9"
classifiers = [
  "Programming Language :: Rust",
  "Programming Language :: Python :: Implementation :: CPython",
  "Programming Language :: Python :: Implementation :: PyPy",
]
dynamic = ["version"]
dependencies = ["polars==1.40.0", "pyarrow"]

[project.optional-dependencies]
dev = ["maturin", "ruff", "mypy", "pre-commit", "pytest", "bump-my-version", "mkdocs-material", "mkdocstrings[python]"]
bench = ["tsfresh", "pandas"]
examples = [
  "plotly",
  "qdrant-client",
  "jupyter",
  "notebook",
  "numpy",
  "scikit-learn",
]
all = ["tsfx[dev,bench,examples]"]

[tool.maturin]
features = ["pyo3/extension-module"]
python-source = "python"


[tool.mypy]
python_version = "3.11"

# mypy per-module options:

[[tool.mypy.overrides]]
module = "tsfx"
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["python_tests"]

[tool.ruff]
line-length = 88
# Assume Python 3.11.
target-version = "py311"
extend-include = ["*.ipynb", "python_tests/"]

[tool.ruff.lint]
# Enable Pyflakes `E` and `F` codes by default.
ignore = [
  "N806",
  "D203",
  "D107",
  "D105",
  "D100",
  "D213",
  "D104",
]
select = [
  "E",
  "W",
  "F",
  "I",
  "UP",
  "B",
  "ANN",
  "S",
  "N",
  "SIM",
  "A",
  "ERA",
  "PD",
  "D",
]
unfixable = ["F401", "F841"]

# Exclude a variety of commonly ignored directories.
exclude = [
  ".bzr",
  ".direnv",
  ".eggs",
  ".git",
  ".hg",
  ".mypy_cache",
  ".nox",
  ".pants.d",
  ".ruff_cache",
  ".svn",
  ".tox",
  ".venv",
  "__pypackages__",
  "_build",
  "buck-out",
  "build",
  "dist",
  "node_modules",
  "venv",
  "python_tests",
]

# Allow unused variables when underscore-prefixed.
dummy-variable-rgx = "^(_+|(_+[a-zA-Z0-9_]*[a-zA-Z0-9]+?))$"


[tool.ruff.lint.per-file-ignores]
"__init__.py" = ["F401", "F403"]

[tool.ruff.lint.mccabe]
# Unlike Flake8, default to a complexity level of 10.
max-complexity = 10

[tool.bumpversion]
current_version = "0.3.3"
commit = true
tag = true
tag_name = "v{new_version}"
message = "Bump version: v{current_version} →  v{new_version}"

[[tool.bumpversion.files]]
filename = "Cargo.toml"
search = 'version = "{current_version}"'
replace = 'version = "{new_version}"'
